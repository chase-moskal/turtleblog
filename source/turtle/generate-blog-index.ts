
import {TurtleOptions} from "../interfaces"
import {BlogPost, GenerateBlogOptions} from "./interfaces"

export async function generateBlogIndex({source, dist, blog, pages, blogPosts}: GenerateBlogOptions): Promise<void> {}
